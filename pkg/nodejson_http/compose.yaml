services:
  app:
    # Name the image.
    image: rendezqueue_nodejson_http
    build:
      context: ../../
      # Relative to context.
      dockerfile: ./pkg/nodejson_http/Dockerfile
    ports:
      - "80:80"
