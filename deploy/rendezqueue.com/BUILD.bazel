load("@aspect_bazel_lib//lib:copy_to_bin.bzl", "copy_to_bin")

copy_to_bin(
    name = "docker_deps",
    srcs = [
        "Dockerfile",
        "entrypoint.sh",
        "index.html",
        "nginx_app.conf",
    ],
)
